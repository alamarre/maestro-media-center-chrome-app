var videoMenu = angular.module('videoMenu',[
    'videoFolderListing'
]);

videoMenu.controller('VideoMenuController', ['$scope','$location', '$compile',
    function($scope, $location, $compile) {
        
    }
]);
